import React from 'react';
import {
    shallow
}
from 'enzyme';

import Logo from './logo';

describe('<Logo />', () => {
    it('Renders without crashing', () => {
        shallow( < Logo / > );
                });
    });
