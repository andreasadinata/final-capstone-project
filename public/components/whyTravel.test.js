import React from 'react';
import {
    shallow
}
from 'enzyme';

import WhyTravel from './whyTravel';

describe('<WhyTravel />', () => {
    it('Renders without crashing', () => {
        shallow( < WhyTravel / > );
                });
    });
